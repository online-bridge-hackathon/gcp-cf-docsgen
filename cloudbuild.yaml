steps:
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
    - functions
    - deploy
    - gcp-cf-docsgen
    - --source=.
    - --trigger-bucket=gba-api
    - --runtime=nodejs12
    - --entry-point=onUpload
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
    - functions
    - deploy
    - gcp-cf-docsgen-ondelete
    - --source=.
    - --trigger-bucket=gba-api
    - --runtime=nodejs12
    - --entry-point=onDelete